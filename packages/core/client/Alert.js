/**
 * show pretty alerts on top page
 * @param  {String} text shown
 */
Kepler.Alert = _.extend(sAlert, {
	//extend this lib https://github.com/juliancwirko/meteor-s-alert
	//TODO show message only one time
	/*once: function(text, type) {
		console.log('once time!')
	}*/
});
